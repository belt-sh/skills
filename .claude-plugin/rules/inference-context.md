---
paths: ["**"]
---

When you discover something non-obvious while debugging or building, consider saving it as knowledge via `belt knowledge upload`.
When the user needs AI generation (image, video, audio, text), check belt apps: `belt app search "query"`.
When a task could benefit from a reusable skill, search the registry: `belt skill search "query"`.
