{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "type": "command",
        "command": "belt-bootstrap.sh --check-update 2>/dev/null; exit 0",
        "timeout": 5000
      }
    ],
    "UserPromptSubmit": [
      {
        "type": "command",
        "command": "belt hook suggest 2>/dev/null; exit 0",
        "timeout": 3000
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "type": "command",
        "command": "belt-bootstrap.sh --check-skill-mutation 2>/dev/null; exit 0",
        "timeout": 3000
      }
    ],
    "Stop": [
      {
        "type": "prompt",
        "prompt": "Review the completed turn. Did the assistant discover something non-obvious worth persisting as knowledge? (debugging insight → observation, architectural pattern → concept, user preference → preference, API behavior → reference). Respond with JSON only: {\"worth_saving\": bool, \"type\": \"...\", \"title\": \"...\", \"content\": \"1-3 sentences\"}",
        "timeout": 15000
      },
      {
        "type": "prompt",
        "prompt": "Was a belt-managed skill used AND improved during this turn? (better error handling, new edge cases, more efficient workflow). Respond with JSON only: {\"improved\": bool, \"skill_name\": \"...\", \"improvement_summary\": \"...\"}",
        "timeout": 15000
      }
    ]
  }
}
